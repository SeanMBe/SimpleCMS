@model IEnumerable<SimpleCMS.Models.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table>
    <tr>
        <th>
            UserName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			@item.UserName
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id })
            &nbsp;|&nbsp;
            @Html.DeleteLink("Delete", @item.Id, @item.UserName)
        </td>
    </tr>
}
</table>

<div class="actions">
    @Html.ActionLink("New Record", "Create")
</div>